export enum ProductStatus {
  CREATED = 1,
  EXAMINING = 2,
  OPEN = 3,
}

export enum ProductDetailStatus {
  USING = 1,
  PENDING = 2,
  EXAMINING = 3,
}

export enum ProductModifier {
  EDITOR = 1,
  AUTHOR = 2,
}
